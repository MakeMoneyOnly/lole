/**
 * SEO utilities for lole Restaurant OS
 */
export {
    SITE_CONFIG,
    defaultMetadata,
    generatePageMetadata,
    generateRestaurantMetadata,
    generateOrderTrackerMetadata,
    generateAuthMetadata,
    generateDashboardMetadata,
    generateRestaurantSchema,
    generateMenuItemSchema,
} from './metadata';
